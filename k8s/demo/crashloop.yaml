apiVersion: v1
kind: Pod
metadata:
  name: demo-crashloop
  namespace: default
  labels:
    app: demo-crashloop
spec:
  restartPolicy: Always
  containers:
    - name: demo
      image: busybox:1.36
      command: ["/bin/sh", "-c", "echo crash && sleep 2 && exit 1"]
